{
  "name": "@changesafe/ai",
  "version": "0.1.0",
  "description": "Provider-agnostic model adapters that produce ChangeProposals. One Zod schema drives structured output on every provider; every provider's output faces identical local validation.",
  "license": "MIT",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "files": [
    "src"
  ],
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@changesafe/core": "0.1.0",
    "@changesafe/domain-network": "0.1.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "typescript": "^5"
  }
}
